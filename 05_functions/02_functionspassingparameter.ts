//function to return string hello
function helloParameter(name: string): string {
  return "Hello" + name; //append the parameter
}

//passing parameter, while calling
console.log(helloParameter("World"));
