var strnum; //union type -> combines any type, here string and number.
strnum = "anu"; //string can be stored
strnum = 123; //also number can be stored, no issue for union type.
// similar to "any type" but here, custom types(specify multiple types).
console.log(strnum); //last stored value will be displayed.
